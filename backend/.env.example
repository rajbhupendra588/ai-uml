# --- Application ---
# development or production
ENVIRONMENT=development
LOG_LEVEL=INFO
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- URLs ---
FRONTEND_URL=http://localhost:3000
API_BASE_URL=http://localhost:8000

# --- AI Models ---
# Required for generating diagrams
OPENROUTER_API_KEY=sk-or-your-api-key
# Default model ID
OPENROUTER_MODEL=arcee-ai/trinity-large-preview:free

# --- GitHub Integration ---
# Optional: Personal Access Token for analyzing public repos with higher rate limits
GITHUB_TOKEN=your_github_pat

# --- Razorpay (Billing / Subscriptions) ---
# IMPORTANT: Key mode must match Plan mode. Use TEST keys with plans created in Test mode.
# In Dashboard: toggle "Test mode" (top) → Settings → API Keys → use Key ID + Secret from same mode.
# For dev/testing use Test keys (rzp_test_...) and create plans in Test mode → Subscriptions → Plans.
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_test_secret
# RAZORPAY_WEBHOOK_SECRET=whsec_xxxx
# Plan IDs from same mode as keys (Test plans for rzp_test_*, Live plans for rzp_live_*)
# RAZORPAY_PLAN_PRO_MONTHLY=plan_xxxx
# RAZORPAY_PLAN_PRO_ANNUAL=plan_xxxx

# --- Database (user login & data persistence) ---
# Default: SQLite (file architectai.db in backend dir). Set SUPABASE_DATABASE_URL to use Supabase.
# DATABASE_URL=sqlite+aiosqlite:///./architectai.db
# Supabase Session pooler (use postgresql+asyncpg for async). Replace [YOUR-PASSWORD]; if password has special chars, URL-encode them.
# SUPABASE_DATABASE_URL=postgresql+asyncpg://postgres.nkcjuwoltqcvaiutpdkj:[YOUR-PASSWORD]@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
# If both DATABASE_URL and SUPABASE_DATABASE_URL are set, Supabase is used (login persisted in Supabase).

# --- Security ---
# Secret key for JWT signing (auth)
SECRET_KEY=dev-secret-change-in-production
